# Sentinel 練習環境：Replica
bind 0.0.0.0
protected-mode no
port 6379
# 初始指向 Master 的固定 IP；故障轉移後 Sentinel 會用 REPLICAOF 指令動態改寫
replicaof 172.28.0.10 6379
replica-read-only yes
appendonly yes
appendfsync everysec
